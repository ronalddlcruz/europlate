import { inventoryService } from '../services/inventory.service.js'
// Contratos de controlador listos; los handlers HTTP se añadirán con las rutas.
export const inventoryController = { inventoryService }
