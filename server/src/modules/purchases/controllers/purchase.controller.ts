import type { Request, Response } from 'express'
import { AppError } from '../../../shared/errors/app-error.js'
import { purchaseInputSchema, purchaseQuerySchema, updatePurchaseSchema } from '../schemas/purchase.schema.js'
import { purchaseService } from '../services/purchase.service.js'
const idFrom = (request: Request) => Array.isArray(request.params.id) ? request.params.id[0] : request.params.id
export const purchaseController = { async list(request: Request, response: Response) { response.json({ data: await purchaseService.list(request.auth!.companyId, purchaseQuerySchema.parse(request.query)) }) }, async catalog(request: Request, response: Response) { const [suppliers, products, warehouses] = await purchaseService.catalog(request.auth!.companyId); response.json({ data: { suppliers, products, warehouses } }) }, async uploadDocument(request: Request, response: Response) { const fileName = request.header('x-file-name'); if (!fileName || !Buffer.isBuffer(request.body) || request.body.length === 0) throw new AppError('PURCHASE_DOCUMENT_INVALID', 'Adjunta un archivo PDF válido.', 422); response.status(201).json({ data: await purchaseService.uploadDocument(request.auth!.companyId, decodeURIComponent(fileName), request.body) }) }, async removeDocument(request: Request, response: Response) { const storageKey = typeof request.body?.storageKey === 'string' ? request.body.storageKey : ''; if (!storageKey) throw new AppError('PURCHASE_DOCUMENT_INVALID', 'No se encontró el archivo adjunto.', 422); await purchaseService.removeDocument(request.auth!.companyId, storageKey); response.status(204).send() }, async get(request: Request, response: Response) { response.json({ data: await purchaseService.getById(request.auth!.companyId, idFrom(request)) }) }, async create(request: Request, response: Response) { response.status(201).json({ data: await purchaseService.create(request.auth!.companyId, purchaseInputSchema.parse(request.body)) }) }, async update(request: Request, response: Response) { response.json({ data: await purchaseService.update(request.auth!.companyId, idFrom(request), updatePurchaseSchema.parse(request.body)) }) }, async receive(request: Request, response: Response) { response.json({ data: await purchaseService.receive(request.auth!.companyId, idFrom(request)) }) }, async remove(request: Request, response: Response) { await purchaseService.remove(request.auth!.companyId, idFrom(request)); response.status(204).send() } }
