import { Ship } from 'lucide-react'
import { DashboardPanel } from '../../../components/dashboard/dashboard-components'

export function ActiveImportsCard({ number, supplier, status }: { number: string; supplier: string; status: string }) {
  return <DashboardPanel title="Importaciones activas" icon={Ship} titleClassName="text-brand"><div className="mb-3 flex items-center justify-between rounded-md border border-blue-100 bg-blue-50/60 px-2.5 py-1.5 text-[10px] text-brand"><span>Seguimiento logístico</span><span className="flex items-center gap-1.5 font-semibold"><i className="h-1.5 w-1.5 animate-pulse rounded-full bg-blue-500" />En curso</span></div><div className="flex min-w-0 items-start justify-between gap-3"><div className="min-w-0"><p className="font-mono text-sm font-semibold text-ink">{number}</p><p className="mt-1 truncate text-xs text-muted">{supplier}</p></div><span className="shrink-0 whitespace-nowrap rounded-full bg-blue-100 px-3 py-1.5 text-[10px] font-semibold text-brand">{status}</span></div><div className="mt-4"><div className="flex items-center justify-between text-[10px] text-muted"><span>Despacho internacional</span><span className="font-medium text-brand">En tránsito</span></div><div className="mt-2 flex items-center gap-1"><i className="h-1.5 flex-1 rounded-full bg-brand" /><i className="h-1.5 flex-1 rounded-full bg-brand" /><i className="h-1.5 flex-1 rounded-full bg-blue-200" /><i className="h-1.5 flex-1 rounded-full bg-slate-100" /></div></div></DashboardPanel>
}
