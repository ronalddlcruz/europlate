import { PackageCheck, TrendingUp } from 'lucide-react'
import { DashboardPanel } from '../../../components/dashboard/dashboard-components'
import type { RotationProduct } from './dashboard-data'

export function RotationSection({ products, periodLabel }: { products: RotationProduct[]; periodLabel: string }) {
  const maxUnits = products[0]?.units ?? 1; const total = products.reduce((sum, product) => sum + product.units, 0)
  return <DashboardPanel title="Productos con mayor rotación" icon={TrendingUp} action={periodLabel}><div className="mb-3 flex items-center justify-between rounded-md border border-blue-100 bg-blue-50/60 px-3 py-2 text-xs text-slate-600"><span className="flex items-center gap-2"><PackageCheck className="h-4 w-4 text-brand" />Un producto por indicador.</span><strong className="font-mono text-brand">{total} und.</strong></div><div className="grid gap-2 sm:grid-cols-2 xl:grid-cols-5">{products.map((product, index) => <article key={product.code} className="rounded-lg border border-border bg-slate-50/50 p-3"><div className="flex items-center justify-between gap-2"><span className="font-mono text-[11px] font-bold text-brand">0{index + 1}</span><span className="rounded-full bg-white px-2 py-1 text-[10px] font-semibold text-slate-600">{product.unit}</span></div><p className="mt-3 min-h-10 text-[13px] font-semibold leading-5 text-ink">{product.name}</p><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-brand" style={{ width: `${product.units / maxUnits * 100}%` }} /></div><div className="mt-2 flex items-end justify-between gap-2"><strong className="font-mono text-sm text-ink">{product.units}</strong><span className="text-[10px] font-medium text-emerald-600">{product.sales}</span></div></article>)}</div></DashboardPanel>
}
